import dotenv from 'dotenv';
dotenv.config();

export const URLS = {
  BASE_URL: 'https://www.wikipedia.org',
};

export const TIME_UNITS = {
  FIVE_SECONDS: 5000,
  TEN_SECONDS: 10000,
  EIGHTY_SECONDS: 80000,
};